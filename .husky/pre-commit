#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged to check staged files
npx lint-staged

# Run unit test suite with coverage to enforce 80% threshold
echo "🧪 Running unit test suite with coverage checks..."
pnpm test:unit:coverage
if [ $? -ne 0 ]; then
  echo "❌ Coverage check failed. All categories must meet 80% threshold:"
  echo "   - Statements: ≥ 80%"
  echo "   - Branches: ≥ 80%"
  echo "   - Functions: ≥ 80%"
  echo "   - Lines: ≥ 80%"
  echo "Please add tests to improve coverage before committing."
  exit 1
fi

echo "✅ All pre-commit checks passed!"
